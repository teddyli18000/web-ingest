# Tasks

Each directory here is one independent Internet collection task.

A task should own its own description, scripts/configuration, and collected data. Keep cross-task coupling to a minimum.

Add new tasks as:

```text
tasks/<task-name>/
├── README.md
└── data/
```

Only add more files when the task actually needs them.
